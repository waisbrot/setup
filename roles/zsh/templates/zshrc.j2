#!zsh

unalias run-help
autoload run-help
HELPDIR=/usr/local/share/zsh/help

setopt NO_BEEP
setopt NO_FLOW_CONTROL

for file in {{rc_path}}/*.zsh; do
    source $file
done

# `less` options: bigger "--more--" line, highlight new line when paging, use a status column, don't beep
export LESS='MwJQ'
